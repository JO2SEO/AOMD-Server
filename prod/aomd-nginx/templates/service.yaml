apiVersion: v1
kind: Service
metadata:
  name: {{ include "aomd-nginx.fullname" . }}
  labels:
    {{- include "aomd-nginx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aomd-nginx.selectorLabels" . | nindent 4 }}
