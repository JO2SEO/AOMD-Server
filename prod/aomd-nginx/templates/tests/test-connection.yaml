apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aomd-nginx.fullname" . }}-test-connection"
  labels:
    {{- include "aomd-nginx.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aomd-nginx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
